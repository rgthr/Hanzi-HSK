(()=>{
const BUILD='20260908.7';
const css=document.createElement('style');css.textContent=`
/* Related shapes: use the same clean UI family as the rest of the app; reserve serif only for the Hanzi itself. */
.family button{font-family:-apple-system,BlinkMacSystemFont,"SF Pro Text","PingFang SC",Arial,sans-serif!important;background:var(--paper)!important}.family .fh{font-family:"PingFang SC","Noto Sans CJK SC",-apple-system,sans-serif!important;font-weight:500!important;font-size:36px!important}.family .fpinyin{font-family:inherit!important;font-size:14px!important;font-weight:650!important;letter-spacing:.01em}.family .fmeaning{font-family:inherit!important;font-size:12px!important}
/* Explore mastery colours */
.wallTile.unseen{background:var(--paper)!important;color:var(--muted)!important}.wallTile.learning{background:#fff7dc!important;color:var(--ink)!important}.wallTile.mastered{background:var(--greenSoft)!important;color:var(--green)!important}.wallTile.struggle,.wallTile.needsWork{background:#fbe9e5!important;color:#8f4638!important}.wallTile.learning:after{background:#c89422!important}.wallTile.mastered:after{background:var(--green)!important}.wallTile.struggle:after,.wallTile.needsWork:after{background:#a64a36!important}
/* Three-way matching test */
.tripleQuiz{padding-top:12px!important}.tripleTitle{font-size:18px;font-weight:760;margin-bottom:5px}.tripleSub{font-size:12px;color:var(--muted);margin-bottom:14px}.tripleGrid{display:grid;grid-template-columns:.72fr 1fr 1.25fr;gap:7px;align-items:stretch}.tripleCol{display:flex;flex-direction:column;gap:7px}.tripleLabel{font-size:9px;letter-spacing:.08em;text-transform:uppercase;color:var(--muted);font-weight:750;text-align:center;margin-bottom:1px}.matchItem{min-height:51px;border:1px solid var(--line);background:var(--paper);border-radius:10px;padding:7px 5px;display:grid;place-items:center;text-align:center;font-size:12px;line-height:1.15}.matchItem.hanzi{font-family:"PingFang SC","Noto Sans CJK SC",sans-serif;font-size:29px}.matchItem.pinyin{font-size:13px;font-weight:650;color:var(--green)}.matchItem.selected{outline:2px solid var(--green);background:var(--greenSoft)}.matchItem.matched{background:var(--greenSoft);border-color:transparent;opacity:.58}.matchItem.wrong{animation:matchShake .22s ease;background:#fbe9e5}@keyframes matchShake{25%{transform:translateX(-3px)}75%{transform:translateX(3px)}}.tripleStatus{text-align:center;font-size:11px;color:var(--muted);margin-top:12px;min-height:16px}
`;document.head.appendChild(css);

function applyExploreColours(){document.querySelectorAll('.wallTile').forEach(el=>{const h=(el.textContent||'').trim(),c=window.byH&&byH[h];if(!c)return;el.classList.remove('unseen','needsWork');const sk=state.skills?.[h]||{};if(state.mastered?.[h])el.classList.add('mastered');else if((sk.mistakes||0)>=2)el.classList.add('needsWork');else if(state.seen?.[h])el.classList.add('learning');else el.classList.add('unseen')})}
new MutationObserver(applyExploreColours).observe(document.body,{childList:true,subtree:true});setTimeout(applyExploreColours,250);

function shuffled(a){return [...a].sort(()=>Math.random()-.5)}
function makeTripleSlide(chars,index){const set=shuffled(chars).slice(0,4),hz=shuffled(set),py=shuffled(set),en=shuffled(set);const slide=document.createElement('div');slide.className='fp quizSlide tripleSlide';slide.dataset.quizIndex=index;slide.innerHTML=`<div class="card"><div class="quizTop">CONNECT · ${index+1}</div><div class="quizBody tripleQuiz"><div class="tripleTitle">Connect the three</div><div class="tripleSub">Match each character with its pinyin and meaning.</div><div class="tripleGrid"><div class="tripleCol"><div class="tripleLabel">Hanzi</div>${hz.map(c=>`<button class="matchItem hanzi" data-h="${c.h}" data-kind="h">${c.h}</button>`).join('')}</div><div class="tripleCol"><div class="tripleLabel">Pinyin</div>${py.map(c=>`<button class="matchItem pinyin" data-h="${c.h}" data-kind="p">${c.p}</button>`).join('')}</div><div class="tripleCol"><div class="tripleLabel">Meaning</div>${en.map(c=>`<button class="matchItem" data-h="${c.h}" data-kind="m">${c.contextMeaning}</button>`).join('')}</div></div><div class="tripleStatus">Tap one item in each column</div></div></div>`;
let selected={};const status=slide.querySelector('.tripleStatus');slide.addEventListener('click',e=>{const b=e.target.closest('.matchItem');if(!b||b.classList.contains('matched'))return;slide.querySelectorAll(`.matchItem[data-kind="${b.dataset.kind}"]`).forEach(x=>x.classList.remove('selected'));b.classList.add('selected');selected[b.dataset.kind]=b;if(selected.h&&selected.p&&selected.m){const arr=[selected.h,selected.p,selected.m],ok=arr.every(x=>x.dataset.h===arr[0].dataset.h);if(ok){arr.forEach(x=>{x.classList.remove('selected');x.classList.add('matched')});const c=byH[arr[0].dataset.h];if(c){bump(c,'recognition',true);bump(c,'meaning',true);bump(c,'sound',true)}selected={};const left=slide.querySelectorAll('.matchItem:not(.matched)').length;if(!left){slide.dataset.answered='1';status.textContent='All connected ✓ · swipe to continue'}else status.textContent='Nice — keep connecting';}else{arr.forEach(x=>{x.classList.add('wrong');setTimeout(()=>x.classList.remove('wrong','selected'),260)});selected={};status.textContent='Not quite — try that connection again';}}});return slide}

/* When the existing quiz is appended, replace two of its four cards with 3-way connection rounds. This preserves two quick recall questions and adds pinyin as a first-class test. */
function upgradeQuiz(){const pager=document.getElementById('pager');if(!pager||pager.dataset.tripleUpgraded)return;const slides=[...pager.querySelectorAll('.quizSlide:not(.tripleSlide)')];if(slides.length<4)return;const chars=(window.focusSet||[]).slice(0,6).filter(Boolean);if(chars.length<4)return;pager.dataset.tripleUpgraded='1';[1,3].forEach((pos,j)=>{const old=slides[pos];if(old)old.replaceWith(makeTripleSlide(chars,j))});}
const obs=new MutationObserver(()=>upgradeQuiz());const pager=document.getElementById('pager');if(pager)obs.observe(pager,{childList:true});setTimeout(upgradeQuiz,300);
window.__hanziBuild=BUILD;
})();